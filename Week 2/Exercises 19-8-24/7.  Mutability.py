name = "John"

name = "john"
print(name)

# You can't change just one character of a string. The whole string has to be re-inputted? re-typed? well re-done, 
# to redefine the value of the variable 
# also strings are not mutable