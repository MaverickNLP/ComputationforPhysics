import sys
i = 42
f = 3.14
b = True
s = "e"

print("Memory usage of int:", sys.getsizeof(i), "bytes")
print("Memory usage of bool:", sys.getsizeof(b), "bytes")
print("Memory usage of float:", sys.getsizeof(f), "bytes")
print("Memory usage of string:", sys.getsizeof(s), "bytes")

# Strings are always the longest, or are they "veritasiaum theme in the bg"